{% extends 'base.html.twig' %}

{% block title %}Roleuser{% endblock %}

{% block body %}
    <h1>Roleuser</h1>
<h1 class="text-center my-5">Utilisateur</h1>
<table class="table mx-auto">
    
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ roleuser.id }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ roleuser.role }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_roleuser_index') }}">back to list</a>

    <a href="{{ path('app_roleuser_edit', {'id': roleuser.id}) }}">edit</a>

    {{ include('roleuser/_delete_form.html.twig') }}
{% endblock %}
